using UnityEngine;

public class LevelSetup : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }
    public virtual void SetLevelGoals()
    {
        
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
